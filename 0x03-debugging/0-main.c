#include "main.h"
#include <stdio.h>
/**
 * main - tests function that prints if intager is positive or negative
 * Return: 0
 */

int main(void)
{
	int zero = 0;

	positive_or_negative(zero);
	return (0);
}

#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Program to print a series of strings
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char z[100] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", z);
	return (0);
}
